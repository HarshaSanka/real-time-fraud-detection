# Raw data

Ignored runtime directory for the two original Sparkov CSV files. Run `fraud-detect data download`.
The raw source includes synthetic names, addresses, and card numbers and must never be committed.
